# Release pipeline

A release folyamat jelenleg GitHub Actions alapú CI pipeline segítségével történik.

A pipeline automatikusan lefut minden push művelet esetén.

A build sikeres lefutása feltétele a release-re alkalmas állapotnak.
